HOTFIX CHECKLIST V4 APLICADO

Qué corrige:
- checklist-diario.ts ahora exporta BloqueChecklist
- ChecklistItem ahora incluye campos que tu HTML y TS ya usan:
  - criticidad
  - detalle
  - referencia
  - orden
- checklist.service.ts ahora genera y normaliza esos campos
- Se mantiene Firestore + fallback localStorage
- Se mantiene la API síncrona que tu pantalla ya esperaba

Problema real que resuelve:
- Tu template ChecklistPageComponent.html usa item.criticidad, item.detalle, item.referencia
- Tu TS usa item.orden y además importa BloqueChecklist
- El modelo anterior no incluía esos campos

Ahora ejecuta:
1) ng build
2) si compila, ng serve
3) prueba Checklist

Si aparece otro error, probablemente ya será un ajuste pequeño de nombres o estructura de bloques.
